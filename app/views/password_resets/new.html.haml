#login_form.well
  %h2.center 发送重置密码邮件
  = simple_form_for @user, url: password_resets_path, method: 'post', html: { class: 'form-horizontal' } do |f|
    = f.input :email
    .form-actions= f.submit '发送重置密码邮件', class: 'btn btn-large btn-danger'